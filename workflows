name: Test Workflow

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:   # allows manual run

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: 23ds3000116@ds.study.iitm.ac.in
        run: echo "Hello, world!"
git add .github/workflows/ci.yml
git commit -m "Add GitHub Action with email step"
git push
